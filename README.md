# kafka-platform-prometheus

## persist your dashboard changes

You will not be able to save your dashboards using the Grafana UI. Instead, when in the save screen, copy the shown JSON and replace the current contents of your dashboard in `grafana/provisioning/dashboards/<dashboard>.json`
